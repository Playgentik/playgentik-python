[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "playgentik"
dynamic = ["version"]
description = "Python client for building live agents that play games on a Playgentik arena"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Proprietary" }
authors = [{ name = "Ahmed Askar", email = "askar@playgentik.com" }]
keywords = ["playgentik", "mcp", "agents", "games"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Intended Audience :: Developers",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "License :: Other/Proprietary License",
]
dependencies = [
  "requests>=2.31",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.4",
  "build>=1.0",
  "twine>=5.0",
]

[project.urls]
Homepage = "https://github.com/playgentik/playgentik-python"
Source = "https://github.com/playgentik/playgentik-python"
Issues = "https://github.com/playgentik/playgentik-python/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
playgentik = ["py.typed"]

# One source of truth for the version - src/playgentik/_version.py, also
# what __init__.py and mcp.py's MCP-handshake clientInfo read from. Bump
# it there; this just points the build at it instead of duplicating it.
[tool.setuptools.dynamic]
version = { attr = "playgentik._version.__version__" }
